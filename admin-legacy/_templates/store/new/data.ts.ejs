---
to: src/store/<%= h.changeCase.lower(name) %>/data.ts
---
import {subState} from '@/store/data';
import {<%= h.changeCase.pascal(name) %>State} from './types';

export const <%= h.changeCase.lower(name) %>State: <%= h.changeCase.pascal(name) %>State = {
  state: {
<% states.split(',').map(s => s.trim()).forEach(function(state) { %>
    <%= state %>: {...subState},
<% }); %>
  },
  data: {
<% states.split(',').map(s => s.trim()).forEach(function(state) { %>
    <%= state %>: null,
<% }); %>
  },
};
